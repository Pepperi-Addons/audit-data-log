{
  "name": "typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ./client-side && npm run build && cd .. && cd ./server-side && npm run build && cd ..",
    "init": "npm i && cd ./server-side && npm i && cd .. && cd ./client-side && npm i --force && cd ..",
    "publish-addon": "npm run build && publish-addon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pepperi-addons/scripts": "^2.0.4"

  }
}
